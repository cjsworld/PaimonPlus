using System;
using System.Collections.Generic;
using XPlugin.Json;

namespace PaimonPlus.Core {
    //public class MonsterModule : CoreEngineModule {

    //    public readonly Dictionary<int, MonsterData> Monsters = new();

    //    public override void Init() {
    //        var descDict = new Dictionary<int, JObject>();

    //        var config = CoreEngine.ReadJArrayConfig("MonsterDescribeExcelConfigData");
    //        foreach (JObject item in config) {
    //            descDict[item["id"].AsInt()] = item;
    //        }

    //        config = CoreEngine.ReadJArrayConfig("MonsterExcelConfigData");
    //        foreach (JObject item in config) {
    //            var monster = new MonsterData(item, descDict);
    //            Monsters.Add(monster.Id, monster);
    //        }
    //    }
    //}
}
